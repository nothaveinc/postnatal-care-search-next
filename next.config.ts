import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  basePath: "/postnatal-care-search-next",
  assetPrefix: "/postnatal-care-search-next",
};

export default nextConfig;
